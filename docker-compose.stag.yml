# docker-compose.stag.yml ─ override only what differs from the base file
services:
  app:
    image: jonathanbechtel/draft_guru:staging   # pull the pre-built release image

  db:
    volumes:
      - draft_guru_postgres_stag:/var/lib/postgresql/data  # separate data dir

volumes:
  draft_guru_postgres_stag:
